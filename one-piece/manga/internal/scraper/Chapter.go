package scraper

import (
	"fmt"
	"op-manga-dl/internal/data"
)

func ParseChapter() (data *data.ChapterData, err error) {
	// TODO: do it just like in ParseMangaList just from some different url with different data

	return nil, fmt.Errorf("under construction")
}
